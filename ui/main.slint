export component MainWindow inherits Window {
    width: 900px;
    height: 600px;
    title: "LibrePOS";

    Text {
        text: "LibrePOS";
        font-size: 32px;
        horizontal-alignment: center;
        vertical-alignment: center;
    }
}
