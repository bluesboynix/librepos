export struct MenuItem {
    id: int,
    name: string,
    code: string,
    price: float,
    category: string,
}

export struct IngredientRow {
    link_id: int,
    ingredient_id: int,
    name: string,
    quantity: float,
    unit: string,
    estimated_cost: float,
}

export struct OrderLine {
    name: string,
    price: float,
    quantity: int,
}

export struct TableCardModel {
    label: string,
    occupied: bool,
    total: float,
}

export struct TableArea {
    id: int,
    name: string,
    tables: int,
    cards: [TableCardModel],
}

export struct ReportSummary {
    total_sales: string,
    order_count: int,
    avg_order: string,
}

export struct ReportItem {
    name: string,
    quantity: int,
    revenue: string,
}

export struct ReportPayment {
    method: string,
    count: int,
    amount: string,
}

export struct ReportDay {
    date: string,
    sales: float,
    sales_display: string,
    orders: int,
}

export struct ReportBill {
    bill_no: string,
    table_name: string,
    area_name: string,
    total: string,
    closed_at: string,
    payment_status: string,
}
